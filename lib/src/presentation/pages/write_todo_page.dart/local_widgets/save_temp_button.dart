import 'package:flutter/material.dart';

class SaveTempButton extends StatelessWidget {
  const SaveTempButton({super.key});

  @override
  Widget build(BuildContext context) {
    return TextButton(onPressed: null, child: Text('임시저장 | 0'));
  }
}
